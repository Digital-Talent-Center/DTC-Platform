; =============================================================================
; PHP-FPM Global Override — DEV ONLY
; Allows PHP-FPM master to run as root so workers can access bind-mounted files
; on Windows Docker Desktop where file ownership is always root:root
; NEVER use this in production!
; =============================================================================

[global]
daemonize = no
